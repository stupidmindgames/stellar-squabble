import React, { Component } from 'react';
import {Col} from 'react-bootstrap';

class Commands extends Component { 
  render() {
    return (<Col className="unit" xs="12" lg="6">Commands</Col>);
  }
}

export default Commands;
