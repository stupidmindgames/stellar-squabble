import React, { Component } from 'react';
import {Col} from 'react-bootstrap';

class Viewer extends Component { 
  render() {
    return (<Col className="unit" xs="12" lg="6">Viewer</Col>);
  }
}

export default Viewer;
